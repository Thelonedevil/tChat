package ml.tchat.plugins.bot.commands.scripts

class DisableScript {
    static void main(String[] args) {
        if (!(args.size() == 1)) {
            // TODO handle invalid args
        }
        else {
            // TODO get object using arg, and disable
        }
    }
}
